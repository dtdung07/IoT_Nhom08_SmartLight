import 'package:flutter/material.dart';

class ChartLight extends StatelessWidget {
  const ChartLight({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Chart Light'),
        actions: [],
      ),
    );
  }
}
